/*
package com.barley.ews;

import microsoft.exchange.webservices.data.core.enumeration.property.WellKnownFolderName;

import java.util.Properties;

*/
/**
 * Created by vikram.gulia on 2/29/16.
 *//*

public class Main {
    public static void main(String[] args) throws Exception {
        Properties properties = new Properties();
        properties.put("exchange.email", "vikram.gulia@dish.com");
        properties.put("exchange.password", "Mdr@12345");

        EmailExchangeService exchangeService = new EmailExchangeService();
        exchangeService.login(properties);
        if (exchangeService.isLoggedIn()) {
            //exchangeService.readEmails(1200, WellKnownFolderName.Root, true);
            exchangeService.findFolder();
        }
    }
}
*/
